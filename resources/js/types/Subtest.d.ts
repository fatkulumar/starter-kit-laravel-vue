import { Subject } from "./Subject";

export interface Subtest {
    id: string;
    subject_id: string;
    subject: Subject;
    subtest_code: string;
    title: string;
    tryout_id: string;
    tryout_id: string;
    amount_question: number;
    amount_minutes: number;
}